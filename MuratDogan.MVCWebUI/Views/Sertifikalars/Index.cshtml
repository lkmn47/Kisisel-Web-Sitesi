@model IEnumerable<MuratDogan.Model.Sertifikalar>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SertifikaAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KurumAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VerilisTarihi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SertifikaAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KurumAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VerilisTarihi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
